#include <stdio.h>

void main() {

    char* myCharPtr = NULL;
    int* myIntPtr = NULL;
    float* myFloatPtr = NULL;

    printf("%p\n", myCharPtr);
    printf("%p\n", myIntPtr);
    printf("%p\n", myFloatPtr);

}