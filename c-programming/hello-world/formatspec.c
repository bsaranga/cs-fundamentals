#include <stdio.h>

int main()
{
    char character = 'g';
    printf("%c", character); // prints out the character 'c'
    printf("%d", character); // prints out the ascii code 103 for lower case 'g'

    printf("%s", "character"); // prints out the string 'character'

    printf("\n");
    return 0;
}